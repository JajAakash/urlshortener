Type npm install to add the dependency

To start  : simply type npm start or nodemon

and go to browser type http://localhost:5100/ to get UI page

if this error-> "ReferenceError: TextEncoder is not defined"  occurs simply go to node_modules->whatwg-url->dist->encoding.js and "const { TextEncoder, TextDecoder } = require("util");" paste this on the top
